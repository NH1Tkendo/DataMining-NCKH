HAUI-tree là cấu trúc cây được sử dụng trong thuật toán HAUP-growth để khai phá các mẫu tiện ích trung bình cao (high average-utility itemsets). HAUI-tree là thuật toán dựa trên cây (tree-based) được đề xuất để giảm số lần quét cơ sở dữ liệu so với các thuật toán dựa trên Apriori như TPAU. Tuy nhiên, HAUI-tree vẫn có nhược điểm là phải tạo ra các mẫu ứng viên (candidate patterns) trong quá trình khai phá